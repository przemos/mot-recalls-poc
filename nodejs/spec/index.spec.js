
let handler = require('../index').handler

describe('Hello World MOT Lambda', () => {

    it('should execute handler', (done) => {
        // handler('event', 'context', (err, success) => {
        //     expect(success.response).toBe('a response')
        //     done()
        // })
        done()
    })
})